server:
  port: 8080

defaultTenant: tenant_1

spring:
  application:
    name: multitenancy
  flyway:
    enabled: false # Desabilitamos a execução automática para gerenciar manualmente.
    locations: classpath:db/migration
#    schemas: tenant_1, tenant_2
#    database: tenant_1, tenant_2
#    baseline-on-migrate: true
